# FontRenderer (Scripted Utility)

Given a TextShape object in the scene, the script replace the text with every character in the alphabet and renders as separated images. Using current render settings.
